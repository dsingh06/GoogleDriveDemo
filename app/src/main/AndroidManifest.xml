<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thatapp.checklists">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ModelClasses.SignatureRecording"></activity>
        <activity
            android:name=".ViewClasses.ProfileActivity"
            android:parentActivityName=".ViewClasses.MainActivity"
            android:windowSoftInputMode="stateHidden" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".ViewClasses.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewClasses.DisplayQuestionsActivity"
            android:parentActivityName=".ViewClasses.DisplayCheckListsActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".ViewClasses.DisplayCheckListsActivity"
            android:parentActivityName=".ViewClasses.MainActivity" />
        <activity
            android:name=".ViewClasses.DisplayReportsActivity"
            android:parentActivityName=".ViewClasses.DisplayCheckListsActivity" />
        <activity
            android:name=".ViewClasses.ViewPdfActivity"
            android:parentActivityName=".ViewClasses.DisplayReportsActivity" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <service
            android:name=".ModelClasses.DriveSyncService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>